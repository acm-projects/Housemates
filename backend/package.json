{
  "name": "housemates-backend",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "sam build",
    "local:start": "sam local start-api",
    "local:invoke": "sam local invoke HelloWorldFunction",
    "deploy": "sam deploy",
    "deploy:guided": "sam deploy --guided"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.152",
    "esbuild": "^0.25.9",
    "typescript": "^5.9.2"
  }
}
