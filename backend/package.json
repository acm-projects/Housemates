{
  "name": "housemates-backend",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "sam build",
    "api": "sam local start-api --profile housemates | jq",
    "deploy": "sam deploy",
    "deploy:guided": "sam deploy --guided"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.152",
    "esbuild": "^0.25.9",
    "typescript": "^5.9.2"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.1009.0",
    "@aws-sdk/client-s3": "^3.1013.0",
    "@aws-sdk/client-scheduler": "^3.1021.0",
    "@aws-sdk/lib-dynamodb": "^3.1009.0"
  }
}
